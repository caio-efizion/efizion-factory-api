PORT=3000
API_KEY=# OBRIGATÓRIO: Defina sua chave de API única e segura aqui
DATABASE_URL="file:./dev.db"
LOG_LEVEL=info
# Exemplo de .env para efizion-factory-api

# Porta do servidor
PORT=3000

# Chave de API para autenticação
# OBRIGATÓRIO: Gere uma chave segura e única. Exemplo: openssl rand -hex 32
# A aplicação NÃO iniciará sem esta variável definida.
API_KEY=

# URL do banco de dados (ajuste conforme necessário)
DATABASE_URL=postgresql://efizion:efizion@localhost:5432/efizion

# Nível de log
LOG_LEVEL=info

# CORS - Origens permitidas (separadas por vírgula)
# Deixe vazio ou remova para permitir apenas localhost:3000 e localhost:3100
CORS_ORIGINS=http://localhost:3000,http://localhost:3100,https://app.efizion.com
